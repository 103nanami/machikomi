<container>
  <div class="row">
    <div class="col-lg-2">
    </div>

    <div class="col-lg-8">
      <div class="confirm_withdraw">
        <h3>削除に該当するデータですか？</h3>
        <p>ユーザーID: <%= @post.user_id %></p>
        <p>ユーザー名: <%= @post.user.username></p>
        <p>氏名: <%= @post.user.last_name %><%= @user.first_name %></p>
        <p>投稿ID: <%= @post.post.id></p>
        <p>投稿タイトル: <%= @post.post_name %>
        <p>投稿データは完全に削除されます。</p>
        <p>よろしいですか？</p>
        <%= link_to "削除する",admin_post_path(@post), method: :delete, class:"btn btn-outline-danger" ,style:"margin-top:50px; width:200px;" %>
        <%= link_to "投稿一覧に戻る",admin_users_posts_path, class:"btn btn-outline-info",style:"margin-top:50px; width:200px;" %>
      </div>
    </div>

    <div class="col-lg-2">
    </div>

  </div>
</container>
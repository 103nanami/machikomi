<container>
  <div class="row">
    <div class="col-lg-12 mx-auto">
      <h1>街一覧</h1>
      <%= @cities.each do |city| %>
        <%=link_to column_path(@column.id) do %>
          <%= city.name %>
        <% end %>
      <% end %>
    </div>
  </div>
</container>
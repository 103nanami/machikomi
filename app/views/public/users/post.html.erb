<conainer>
  <div class="row">
    <div class="col-lg-12 mx-auto">
      <h1>自分の投稿</h1>

      <%= @user.each do |user| %>
        <%= attachment_image_tag user :image %>
        <%= user.post.post_name %>
        <%= user.post.post_text %>
      <% end %>

    </div>
  </div>
</conainer>